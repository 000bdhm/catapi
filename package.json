{
  "name": "catapi",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Random cat GIF serverless function — always returns a different cat GIF",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "tweetnacl": "^1.0.3"
  }
}
